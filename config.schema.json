{
  "pluginAlias": "Technotherm",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Technotherm"
      },
      "username": {
        "title": "Username",
        "type": "string",
        "required": true
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "format": "password"
      },
      "home": {
        "title": "Home",
        "type": "string",
        "required": false,
        "description": "The name of the home you want to restrict Homebridge to displaying. Only required if there's more than one home set up under your account."
      },
      "apiName": {
        "title": "API Name",
        "type": "string",
        "required": true,
        "default": "api-lhz",
        "description": "Name of API endpoint to use. Default is probably ok."
      },
      "clientId": {
        "title": "Client ID",
        "type": "string",
        "required": true,
        "default": "54bccbfb41a9a5113f0488d0",
        "description": "Client ID is repurposed from an existing web client or app and shared amongst all users."
      },
      "clientSecret": {
        "title": "Client Secret",
        "type": "string",
        "required": true,
        "default": "vdivdi",
        "description": "Client Secret is repurposed from an existing web client or app and shared amongst all users."
      }
    },
    "required": [
      "name",
      "username",
      "password",
      "apiName",
      "clientId",
      "clientSecret"
    ]
  }
}
